# Merine Rebuild 协作入口

## 项目立意与当前阶段

本项目的唯一目的：**项目全生命周期及全栈学习**。以海防业务为练习场景，在独立 monorepo 中走通需求、设计、数据建模、前后端实现、测试、交付与维护。优先交付能解释、能复现、能验证的一条完整链路，以学习价值和业务正确性衡量进展。

采用 React + TypeScript + Ant Design、Java 21 + Spring Boot + MyBatis、MySQL 的模块化单体。具体版本以仓库清单、锁文件和容器配置为准。旧项目用于理解业务，不要求照搬旧菜单、代码或架构；生产替换与真实数据迁移另立范围。

截至 2026-09-19，实际实现为 **M1.1 基础联调工程加登录闭环与用户管理模块**：登录、退出、当前身份与 CSRF 由本项目的 Java API 与本地 MySQL 真实完成（Spring Security Session + HttpOnly Cookie，账号在 `sys_user`，密码 BCrypt 哈希），用户管理的查询、新建、编辑与启用停用也已接真实接口。应用只有**首页、用户管理、工程诊断**三个页面。

`docs/sys-design` 下的设计稿**只作样式与交互参考**，不是待实现模块的清单。

**尚未实现**：功能权限与数据范围模型。授权目前只有一道门——角色编码在 `merine.security.user-admin-role-codes`（默认 `SYSTEM_ADMIN`）里的账号才能调用用户管理接口；角色能使用什么功能、能看哪些业务数据仍未定义。此外还有图谱查询、任务调度、AI 接入与情报流转。设计稿和重建计划描述的能力不等于已实现；实施缺口见[规则索引](docs/rules/README.md)。

## 开始任务

1. 检查 Git 状态，保护已有及未跟踪文件；从当前入口和受影响链路定位，按下表只读相关规则。
2. 对业务改动先说清场景、权限、状态、不变量和验收方式；复杂链路可用简短表格或示例，普通修改直接执行，不强制逐任务建文档。
3. 完成范围内实现和必要验证。关键设计解释“为什么这样做、代价是什么”，让维护者能追踪页面 → API → SQL；不为教学堆砌概念或拖延已授权工作。
4. 交付说明改了什么、验证了什么、剩余缺口。区分静态检查、真实运行、替身验证和未验证，检查通过后结束，不顺带重构。

## 不可省略的约束

- **环境隔离**：只用本项目本地容器与合成数据；不读取同级生产 dump，不连接旧库、生产或外部 AI 服务，不输出 `.env`、密码或凭据。查旧源码需要任务中的明确范围，不能顺着归档文档链接自行遍历同级仓库。
- **操作授权**：提交、推送、删卷、清库、重灌，以及新增外部写入、真实数据传输或付费调用，需要对应明确授权。同一范围内已有授权持续有效。
- **新增依赖先询问**：第三方运行库、开发/测试依赖、构建插件和新基础服务都适用。先给出具体问题、现有方案为何不足、拟引入内容及收益与成本，获得确认后再改依赖、锁文件或安装。计划、设计稿、示例中的库名不算授权，已有传递依赖也不算已批准的直接依赖。
- **沉淀规则先告知**：开发中需要新增或实质调整规范、模板、自动约束或 Skill，先说明原因、位置和影响，再落实到合适文件；已明确要求的沉淀直接推进。项目规则留在 `docs/rules`，不擅自改全局规则或安装全局 Skill。
- **简洁且有边界**：组件、函数、类和模块各有一个清晰职责。复用真实重复的业务规则和稳定能力；不建立万能组件、空壳层、全套 Base 类或只为单一实现服务的接口。可读性优先于短代码和抽象数量。
- **权限在后端**：菜单和按钮只改善体验；列表、详情、统计、导出及写入都校验操作权限与数据范围。系统管理权限不代表全部业务数据可见。
- **契约一致**：后端 DTO、校验和接口声明定义契约，经本地 OpenAPI 导出快照与 TypeScript 类型。生成文件不手改；运行时 JSON 仍须与契约核对。
- **数据可靠**：每张表和每个字段有有效注释；表结构用新增 Flyway 迁移，已执行迁移不改写。运行账号只持有 DML 权限。关键写入明确事务、前置状态和并发/重复请求语义。
- **按风险测试**：复杂、存疑或失败代价高的逻辑写有意义的测试；简单展示、直通代码和文档不为覆盖率补测试。数据库语义用隔离的真实 MySQL 验证，不能用 mock 或编译代替。

## 按任务读取

| 任务                               | 入口                                                                                            |
| ---------------------------------- | ----------------------------------------------------------------------------------------------- |
| 首次了解、启动、命令               | [README](README.md)                                                                             |
| 编码原则、依赖与规范治理、当前缺口 | [规则索引](docs/rules/README.md)                                                                |
| 页面、组件、主题和交互             | [前端规则](docs/rules/frontend.md)，再读目标页面设计文件                                        |
| Java、接口、安全、事务、任务       | [后端规则](docs/rules/backend.md)                                                               |
| 表、字段、索引、SQL 迁移           | [数据库规则](docs/rules/database.md)                                                            |
| 新文件、模块边界、跨模块复用       | [目录与模块规则](docs/rules/structure.md)                                                       |
| 选择测试、检查和验收               | [测试规则](docs/rules/testing.md)                                                               |
| 学习路线、范围与里程碑背景         | [重建计划](docs/archive/rebuild-plan-2026-09-19.md)，按章节读取；归档中的旧路径不是当前工程入口 |

前端在 `apps/web`，后端在 `apps/api`，生成契约在 `packages/api-contract`，运行配置在 `infra`。不要为普通任务通读所有规则、归档或设计文件。

## 运行与完成

- 默认 `./scripts/dev.sh up` 启动；`status`、`logs` 检查实际状态，`down` 停止并保留数据卷。启动先迁移，迁移失败即停止；不以删卷重试处理未知状态。
- Java 使用本项目容器内 JDK 21；Node / pnpm 遵循仓库配置。不改旧项目环境。
- 按影响运行 `./scripts/dev.sh check`、`./scripts/dev.sh build`、契约生成、真实接口或浏览器验证；具体能力与限制见[测试规则](docs/rules/testing.md)。纯文档变更只核对内容、链接、格式和差异。
- 当前基础工程不等于已满足全部新规则；涉及相应链路时补齐，不借规范落地之名一次改完所有模块。
