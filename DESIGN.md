# 海防研判工作台 · 前端设计规范

> 首次整理：2026-09-20。适用于 Merine Rebuild 后续页面、新增组件与已有界面调整。
> 基线来自当前 React 应用和初始 UI 原型；文中的“约定”指导后续开发，不代表所有现有页面都已完成视觉与无障碍验收。

## 1. 使用方式与依据

先确定页面是查询列表、树与详情、工作区还是登录入口，再选用本文的布局与组件。取值使用应用内已有 token；没有覆盖的业务按实际需求设计，稳定后再补入本文。

| 资料                                                                                   | 负责什么                                     | 使用方式                                     |
| -------------------------------------------------------------------------------------- | -------------------------------------------- | -------------------------------------------- |
| 本文                                                                                   | 视觉语言、尺寸、页面模式、交互表现与设计验收 | 前端设计的首要入口                           |
| [应用 tokens](apps/web/src/app/theme/tokens.css)                                       | 亮暗配色、字体、间距、基础尺寸的实际定义     | CSS 使用这里已有的变量名；完整值以此文件为准 |
| [Ant Design 映射](apps/web/src/app/theme/antdTheme.ts)                                 | 组件库主题与尺寸配置                         | 调整标准控件先看这里                         |
| [全局样式](apps/web/src/app/theme/global.css)                                          | 重置、基础排版、焦点与减弱动效               | 页面局部样式放 CSS Modules                   |
| [前端规则](docs/rules/frontend.md)                                                     | 组件职责、权限、查询、表单、缓存与错误处理   | 实现时配合本文使用                           |
| [原型总览](docs/sys-design/index.html)、[控件标本](docs/sys-design/design-system.html) | 初始布局、控件及交互样例                     | 按目标页面查阅，不作为待实现功能清单         |

当前基线包括登录、应用外壳、首页、用户、角色、菜单、单位、字典及工程诊断。图谱、AI、任务、情报和数据范围仍未实现，相关原型只提供方向。当前功能与运行方式见 [README](README.md)。

数值冲突时，先核对应用 token、主题映射和目标组件的实际样式，再修正文档；原型中的示例代码、演示数据、技术选型和权限描述不定义正式接口。明确的新需求可以调整基线，但应同步维护实现和本文，不再维护另一份通用视觉规范。

## 2. 视觉方向

**专业、清晰、克制、有秩序的数据工作台。** 中性蓝灰承担大面积背景，蓝色强调操作与选中关系，信息层级来自对齐、排版和边界。

| 原则       | 落到页面上                                                                                 |
| ---------- | ------------------------------------------------------------------------------------------ |
| 信息优先   | 让用户先看到对象、状态、所属关系和可执行动作；不为填满页面添加无业务意义的统计卡           |
| 便于核对   | 编号、时间、计数用等宽数字；长名称可读，状态有文字，错误有原因                             |
| 操作有层级 | 同一操作区突出一个主要动作，其他使用默认或文字按钮；焦点、选中和状态反馈不计入“主按钮数量” |
| 分层克制   | 以 `--bg`、`--surface`、边框分层；普通内容卡无重阴影，浮层才需要抬升感                     |
| 状态可信   | 加载、无匹配、无数据、失败、无权限分别表达；不能用旧数据或演示数据伪装成功                 |
| 布局稳定   | hover 不改变几何尺寸；切换主题保留输入与查询；面板打开后仍能知道正在处理谁                 |

品牌名使用“海防研判工作台”，标识复用 [brand-mark.svg](apps/web/public/brand-mark.svg)：导航中 28px，登录页中 40px。功能图标使用已安装的 `@ant-design/icons`，导航图标默认由[路由注册表](apps/web/src/app/routeRegistry.tsx)给出；目录与页面可在菜单管理里从[图标注册清单](apps/web/src/app/iconRegistry.tsx)另行选择，未配置时回落到默认图标。图标跟随文字颜色；纯图标按钮提供可理解的名称与提示。

不沿用旧项目的深藏青顶栏、emoji 图标或旧组件类名。业务区不添加渐变背景、发光边框、卡片悬浮位移和装饰性入场动画；登录页已有港口配图是独立入口的视觉资产。

## 3. 色彩与双主题

下列名称都是应用中真实存在的 CSS 变量。亮色定义在 `:root`，暗色在 `:root[data-theme='dark']`；组件按语义取色，不在页面中复制 Hex。

### 3.1 背景、文字与边界

| Token              | 亮色      | 暗色      | 用途                             |
| ------------------ | --------- | --------- | -------------------------------- |
| `--bg`             | `#f4f6fa` | `#0b1220` | 页面底色                         |
| `--surface`        | `#ffffff` | `#111c2d` | 内容卡、抽屉、弹窗、下拉表面     |
| `--surface-2`      | `#f8fafd` | `#19263b` | 表头、次级区域                   |
| `--fg`             | `#172033` | `#e5edf8` | 正文、标题、关键值               |
| `--muted`          | `#59677a` | `#a7b4c8` | 次要说明、辅助信息               |
| `--fg-placeholder` | `#6c7789` | `#8395ae` | 占位与更弱的信息层级             |
| `--border`         | `#dce3ee` | `#2c3e57` | 内容分隔、卡片边框               |
| `--border-control` | `#7e8aa0` | `#5e7396` | 输入框、默认按钮、可选择卡片边界 |
| `--border-strong`  | `#4c5a70` | `#7c8fae` | 需要加强的控件边界               |
| `--nav-bg`         | `#f8fafd` | `#0e1829` | 侧栏                             |
| `--nav-selected`   | `#e4edfd` | `#1c2e49` | 当前导航项                       |
| `--nav-hover`      | `#edf1f8` | `#16233a` | 导航悬停                         |

`--border` 是装饰分隔线，不能作为输入控件唯一的识别边界。重要业务值即使是第二行，也应清楚可读；不能用禁用色弱化仍需核对的信息。

### 3.2 操作与交互

| Token                  | 亮色      | 暗色      | 用途                     |
| ---------------------- | --------- | --------- | ------------------------ |
| `--accent`             | `#2563eb` | `#60a5fa` | 主操作、链接、强调       |
| `--on-accent`          | `#ffffff` | `#0b1220` | 自定义实心主色控件的前景 |
| `--accent-hover`       | `#1d4fd0` | `#8cbdfb` | 主色悬停                 |
| `--accent-active`      | `#173fa8` | `#a9cffc` | 主色按下                 |
| `--accent-soft`        | `#eaf1fe` | `#16294a` | 浅主色区域               |
| `--accent-soft-border` | `#a9c4f5` | `#3c5c93` | 主色选中边界             |
| `--surface-hover`      | `#edf1f8` | `#1b2942` | 普通可操作表面悬停       |
| `--surface-active`     | `#e4eaf4` | `#22314d` | 普通表面按下             |
| `--surface-selected`   | `#e4edfd` | `#1e3355` | 列表或卡片选中           |
| `--row-hover`          | `#f3f6fb` | `#1a2740` | 表格行悬停               |
| `--focus`              | `#2563eb` | `#93c5fd` | 键盘焦点                 |
| `--disabled-bg`        | `#eef1f6` | `#1e2a3d` | 禁用背景                 |
| `--disabled-fg`        | `#9ba5b4` | `#586a85` | 禁用前景                 |

自定义控件切换实心背景时，前景和背景成对调整。暗色主色表面使用深色 `--on-accent`，不能机械沿用白字；Ant Design 控件的实际前景还需核对组件主题，见第 10 节。

### 3.3 状态色与分类色

| 语义             | 前景 token（亮 / 暗）              | 背景 token（亮 / 暗）                     |
| ---------------- | ---------------------------------- | ----------------------------------------- |
| 成功、完成       | `--success`：`#1a7a45` / `#57ce8a` | `--tag-success-bg`：`#e6f4ea` / `#17352a` |
| 警示、需留意     | `--warning`：`#8a5a08` / `#f0bc5a` | `--tag-warning-bg`：`#fbf1df` / `#3a2c14` |
| 失败、破坏性操作 | `--danger`：`#b3261e` / `#f58c84`  | `--tag-danger-bg`：`#fceae8` / `#3d1e1c`  |
| 中性分类         | `--fg`                             | `--tag-neutral-bg`：`#eef1f6` / `#232f44` |
| 一般提示         | `--fg`，图标可用 `--accent`        | `--tag-accent-bg`：`#eaf1fe` / `#16294a`  |

标签边框使用 `--tag-border`。颜色要与状态文字一起出现，不把红色用作一般重要性标记。

当前系统管理的 [DictTag](apps/web/src/features/dictionaries/components/DictTag.tsx) 采用**中性标签为主、`DISABLED` 使用警示底色**，两种都用 `--fg` 文字；不是所有“启用”都显示绿色。状态、菜单类型、单位层级的标签来自字典，业务判断仍按取值，停用项仍能解析历史标签。不要另写一份中文与颜色映射。

图谱实体另有 `--entity-person`、`--entity-vessel`、`--entity-place`、`--entity-event` 及各自 `-soft` 背景，供未来实体识别使用；它们不表示风险等级，也不是系统管理标签的色轮。

## 4. 排版、间距与尺寸

### 4.1 字体与文字层级

标题用 `--font-display`，正文用 `--font-body`，编号、时间和计数用 `--font-mono`。完整系统字体回退栈见应用 tokens；这些字体名不表示已内置字体文件，不因设计字体名称自动接入 CDN 或字体包。

| Token                | 值        | 常见用途                                      |
| -------------------- | --------- | --------------------------------------------- |
| `--fs-11`            | 11px      | 紧凑标签、计数附注，不用于大段正文            |
| `--fs-12`            | 12px      | 表头、辅助说明、次级编号                      |
| `--fs-13`            | 13px      | Ant Design 基准字号、表格正文、表单与搜索标签 |
| `--fs-14`            | 14px      | 全局正文基准、品牌小标题、一般分区标题        |
| `--fs-16`            | 16px      | `PageHeader`、抽屉和确认弹窗标题              |
| `--fs-18`、`--fs-20` | 18 / 20px | 已有扩展档，按实际信息层级使用                |
| `--fs-24`            | 24px      | 登录品牌名、登录标题                          |

- 字重使用 `--fw-read: 400`、`--fw-ui: 500`、`--fw-strong: 600`。
- 行高使用 `--lh-display: 1.36`、`--lh-title: 1.4`、`--lh-ui: 1.5`、`--lh-body: 1.75`；长说明用正文行高，紧凑表格用 UI 行高。
- 编号、日期、计数使用 `font-variant-numeric: tabular-nums`，必要时复用全局 `.mono`。等宽字距为 `--ls-mono: 0.02em`；中文标题不用负字距。
- 不根据选中态随意放大字号或加粗整行，以免布局抖动。已有分页当前页的强调字重可以保留。

### 4.2 间距与表面

| Token | `--sp-1` | `--sp-2` | `--sp-3` | `--sp-4` | `--sp-5` | `--sp-6` | `--sp-7` | `--sp-8` | `--sp-9` | `--sp-10` |
| ----- | -------- | -------- | -------- | -------- | -------- | -------- | -------- | -------- | -------- | --------- |
| 值    | 4px      | 8px      | 12px     | 16px     | 20px     | 24px     | 32px     | 40px     | 48px     | 64px      |

常见页面内容左右与底部留 16px，区块间距 12px，按钮组 8px，行内紧凑动作 4px；表面内边距通常 12px 或 16px。2px、6px、10px 的既有光学微调可以局部保留，不另建一套间距体系。

| 类别         | 现有值与用途                                                                                                                          |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------- |
| 圆角         | `--r-1: 2px`；`--r-2: 4px` 用于紧凑标签与分页；`--r-3: 6px` 用于控件；`--r-4: 8px` 用于内容卡；`--r-pill: 999px` 用于需要的圆形或胶囊 |
| 普通卡片     | `--surface` + `1px solid var(--border)` + `--r-4`，优先不加阴影                                                                       |
| `--shadow-1` | `0 1px 2px rgba(23, 32, 51, 0.06)`，微弱抬升                                                                                          |
| `--shadow-2` | `0 2px 8px rgba(23, 32, 51, 0.08), 0 0 0 1px rgba(23, 32, 51, 0.04)`，小浮层参考                                                      |
| `--shadow-3` | `0 12px 32px rgba(23, 32, 51, 0.16), 0 0 0 1px rgba(23, 32, 51, 0.06)`，大浮层参考                                                    |

Ant Design 浮层由组件库主题管理，不必逐个覆盖成上述阴影；自定义浮层复用现有值。阴影不能代替暗色主题里的表面边界。

### 4.3 常用尺寸

| 对象         | 基线                        | 说明                                                                 |
| ------------ | --------------------------- | -------------------------------------------------------------------- |
| 应用导航     | 224px / 折叠 64px           | `--nav-w` / `--nav-w-min`；当前 AppShell 也显式配置了这些数字        |
| 顶栏         | 56px                        | `--topbar-h`；与导航品牌区等高                                       |
| 导航项       | 36px                        | Ant Design Menu 的 `itemHeight`                                      |
| 控件         | 默认 32px、小 28px、大 40px | 对应主题 `controlHeight` 系列；登录等主要表单可使用大规格            |
| 表格         | 表头 40px、普通数据行 44px  | 13px 正文；多行标签、树表和字典表允许由内容撑高，不裁切内容去凑 44px |
| 分页按钮     | 30px 高、最小 30px 宽       | 当前 `TablePager` 的紧凑规格                                         |
| 确认弹窗     | 440px                       | 使用 `ConfirmDialog`                                                 |
| 简单表单抽屉 | 480px                       | 字典、重置密码                                                       |
| 中等表单抽屉 | 520px                       | 单位、菜单                                                           |
| 授权表单抽屉 | 560px                       | 用户、角色                                                           |
| 只读成员抽屉 | 640px                       | 角色成员清单                                                         |

这些值按当前用途选取，不把某页列宽或抽屉宽度硬套到所有页面。尺寸不能超过可用视口；新增尺寸应有内容需求，而不是让相同控件各自长出一套规格。

## 5. 页面布局

### 5.1 应用外壳

[AppShell](apps/web/src/app/AppShell.tsx) 统一提供导航、面包屑、主题切换与身份入口。应用高度为 `100vh`，最小宽度 1366px；外壳不滚动，工作区负责页面纵向滚动。表格横向滚动限于表格容器。

以 **1440×900、1366×768** 为当前桌面验收尺寸。导航首次打开在宽度 ≤1439px 时默认折叠；已有 `merine.nav` 偏好优先，当前实现不是随 resize 强制折叠。不要把原型的 CSS 断点行为写成应用事实。

登录即可见的首页是固定入口，其余业务导航和面包屑依据 `/api/me/menus` 与已注册路由生成。正式侧栏不加入原型中的“交付件”“设计系统”或未实现页面。

### 5.2 查询列表

顺序为 **页面标题与主要动作 → 查询区 → 列表工具栏 → 表格 → 分页**。参考 [用户页](apps/web/src/features/users/UserListPage.tsx) 和 [角色页](apps/web/src/features/roles/RoleListPage.tsx)。

- 标题复用 `PageHeader`，名称简洁，说明只保留用户完成操作需要的背景。
- 查询和列表各自成为带边框的表面，页面块间距 12px，不再外套一层装饰卡片。
- 工具栏左侧为列表名称与总数，右侧为已选数量、批量操作、刷新等；整页新建入口在标题区。
- 当前列表随工作区纵向滚动，并非全都使用固定表头与表体内滚动。数据量确需固定表头时，明确滚动容器，避免同一列表出现两套纵向滚动。

### 5.3 树与主从内容

按对象关系决定布局，保持左侧定位、右侧处理，不把所有管理页强行做成同一种表格。

| 场景 | 当前实现                                                                                                  | 交互重点                                             |
| ---- | --------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| 单位 | [单位页](apps/web/src/features/units/UnitListPage.tsx)：左侧树 360px，右侧 `minmax(0, 1fr)`；填满剩余高度 | 左右面板各自滚动，清楚标出选中单位与所属关系         |
| 字典 | [字典页](apps/web/src/features/dictionaries/DictionaryListPage.tsx)：左栏 320–380px，右栏弹性宽度         | 类型是入口，字典项是操作对象；右侧标题显示当前类型   |
| 菜单 | [菜单页](apps/web/src/features/menus/MenuListPage.tsx)：层级表格与维护抽屉                                | 区分目录、页面、页签、按钮；路由绑定与权限码分别表达 |

改变左侧对象时清理不属于新对象的选中项和草稿。详情加载失败要显示失败，不能仍用上一个对象的数据冒充当前对象。

### 5.4 登录、首页与诊断

[登录页](apps/web/src/features/auth/LoginPage.tsx) 是独立布局：左侧港口配图，右侧登录区；右栏宽 `clamp(480px, 38vw, 640px)`，品牌头、主题工具条与表单共用最大 420px 宽度。≤959px 隐藏配图，≤479px 缩小留白，低于 780px 高度时压缩纵向间距；暗色下配图降低亮度与饱和度。

登录页的窄屏适配不代表整个业务系统已支持移动端。复用 [login-harbor.jpg](apps/web/public/login-harbor.jpg)，不将图片扩散成管理页面背景。凭据错误留在表单附近，提交时有明确等待状态。

首页以当前身份与可用入口为主；工程诊断服务联调，其技术信息与简化表单不作为正式业务页面的设计模板。

## 6. 组件与操作

### 6.1 标题、按钮与查询

- `PageHeader` 只承载标题、必要说明和右侧动作；查询、权限与请求逻辑留在业务模块。正式页面按实际用途设置 `demo`，不把组件默认的演示标签变成品牌副标题。
- 主要动作使用 `Button type="primary"`，一般动作使用默认按钮，行内动作使用小号文字按钮；破坏性动作使用 `danger` 并明确后果。
- 行内操作常驻，不只在 hover 时出现；图标不能代替可理解的动作名称。
- 查询字段在控件上方显示标签，组间距 12px。用户页当前字段列最小 232px、操作列 248px，是该页的布局参考，字段数量变化时重新核对空间。
- “更多条件”收起后已填条件仍有效；查询、重置、更换条件回第一页。输入中的值与已提交条件分开，不能输入一半就把列表悄悄换掉。
- 选项加载失败给出原因和重试，不能表现成“只有全部可选”。

### 6.2 表格与分页

标准表格使用 Ant Design `Table`；列与业务渲染留在 feature 内。参考 [UserTable](apps/web/src/features/users/components/UserTable.tsx)，无需先抽象万能表格。

| 区域         | 约定                                                                                             |
| ------------ | ------------------------------------------------------------------------------------------------ |
| 表头         | `--surface-2` 背景、`--muted` 文字、12px / 500；单元格左右通常 12px                              |
| 数据行       | 13px 正文，hover 用 `--row-hover`，选中用 `--surface-selected`                                   |
| 编号与时间   | 等宽数字；需要逐位核对的内容不任意换行或省略                                                     |
| 长名称与标签 | 列宽按内容确定；允许多行的字段自然增高，截断时提供可用的完整内容入口                             |
| 操作列       | 右对齐，宽度按实际动作数量确定，避免按钮挤成两行；无可用动作时隐藏整列                           |
| 横向空间不足 | 表格容器内滚动，不压缩到数据无法阅读；`scroll.x` 按本表列宽确定                                  |
| 分页         | 复用 [TablePager](apps/web/src/shared/ui/TablePager.tsx)：左侧总数、每页条数和实际区间，右侧页码 |

当前 `TablePager` 没有切换每页条数的控件，不照搬原型或旧项目中的完整分页器。页码从 1 开始，总数和当前页行数由接口提供；翻页后清理已失效的选择。

### 6.3 表单与抽屉

以 Ant Design `Form` 的垂直布局为主：标签在上，控件在下，必要的格式说明紧邻字段。复杂表单按基本信息、角色或权限等真实职责分段，不为视觉分区制造额外概念。

- 新建和编辑在标题中区分，编辑时显示目标名称；不可变标识说明原因，不伪装成可编辑字段。
- 必填和长度约束由表单表达，字段错误显示在字段下方，整体失败显示在抽屉内；失败保留输入。
- 提交按钮进入 loading，防止重复提交。创建成功后刷新相关列表；不要未收到结果就宣称保存成功。
- 打开后焦点进入可操作字段，关闭后回到触发入口；关闭与更换对象时清理草稿，避免串用数据。
- 抽屉打开时保留底下的查询与列表位置。重要未保存输入需要离开提醒，不能只依赖 `destroyOnHidden`；当前覆盖情况见第 10 节。
- 角色选项、单位选择使用对应模块公开组件；枚举选项走字典。字典标识创建后不可改，类型和条目只启停，不放删除按钮。

### 6.4 确认与反馈

| 场景                       | 使用方式                                                                                                 |
| -------------------------- | -------------------------------------------------------------------------------------------------------- |
| 启停、删除、影响授权的操作 | 使用 [ConfirmDialog](apps/web/src/shared/ui/ConfirmDialog.tsx)，写明对象、影响范围、会话或关联数据的变化 |
| 普通保存                   | 不额外套一层无意义确认；成功后用当前 Ant Design App 上下文中的 message 轻提示                            |
| 字段校验                   | 字段下方错误，不用全局 Toast 代替                                                                        |
| 表单提交失败               | 留在表单内，保留输入与后续操作                                                                           |
| 页面或刷新失败             | `Alert` / `Result` + 可理解的原因 + 重试入口；需要诊断时附请求编号                                       |

`ConfirmDialog` 当前为 440px 居中弹窗，遮罩不可点击关闭；提交期间取消和 Esc 不可用，失败留在弹窗里。调用方也应防止提交期间关闭。确认按钮使用“禁用账号”“删除角色”等具体动作，不用模糊的“是”。

## 7. 状态与权限的呈现

| 状态               | 页面表现                                                   |
| ------------------ | ---------------------------------------------------------- |
| 首次加载           | 对应区域的骨架或等待提示，不先展示“暂无数据”               |
| 后台刷新           | 保留现有结果，刷新入口显示等待状态                         |
| 无数据             | 说明还没有什么对象；有创建权限时提供建立入口               |
| 无匹配             | 说明筛选无结果，提供放宽或清空条件入口                     |
| 首次请求失败       | 失败原因与重试；不用空列表代替                             |
| 有旧结果但刷新失败 | 明确“刷新失败，以下为上一次的结果”，不要把旧结果冒充最新值 |
| 无页面权限         | 显示 403，不展示业务数据，不直接退出登录                   |
| 会话失效           | 统一身份失效处理，避免多个请求各弹一次提示                 |
| 写入中或被拒绝     | 操作显示等待或拒绝原因；以服务端结果决定最终状态           |

没有操作权限的动作不渲染；一行所有动作均不可见时隐藏操作列。**承载状态的信息仍要保留**：没有启停权限时，开关保留并禁用，说明原因；业务前置条件不满足时，动作保留、禁用并说明，例如“仍有成员，不能删除”。

菜单隐藏只是导航表现，真正的授权在后端。当前功能权限不代表业务数据范围；不能照原型加入任意切换单位、模拟越权或没有真实支持的范围选择器。

## 8. 文案、键盘与可读性

### 8.1 文案

| 内容       | 约定                                                                                         |
| ---------- | -------------------------------------------------------------------------------------------- |
| 标题       | 对象或任务名称，如“用户管理”“编辑角色”                                                       |
| 按钮       | 动词或动词加对象，如“查询”“新建用户”“重置密码”                                               |
| 成功       | 指明已完成动作，如“角色已保存”，少用没有对象的“操作成功”                                     |
| 空值       | 优先表达业务含义，如“从未登录”“未分配角色”；确实无值时统一在该字段中使用 `—`，不把未知写成 0 |
| 日期时间   | 管理列表使用本地时区 `YYYY-MM-DD HH:mm`，跨时区业务明确标注；不靠“几分钟前”完成核对          |
| 错误       | 原因、可做的下一步，必要时请求编号；不显示堆栈、SQL、凭据或原始技术异常                      |
| 状态和分类 | 使用字典标签，不在页面另维护中文翻译；未知取值保留可辨认的回退内容                           |

用户界面不放设计标注、接口路径、缓存策略或实现教程。合成数据和环境来源在开发说明中维护，不把“本地联调”等字样挂在品牌名下。

### 8.2 键盘、焦点与动效

- 操作用真正的按钮和链接，输入有 label；键盘顺序与视觉顺序一致，纯图标操作有 `aria-label`。
- 自定义可聚焦元素沿用全局 `:focus-visible`：`2px solid var(--focus)`，偏移 2px；不能为了去掉蓝框而移除焦点反馈。
- 抽屉和弹窗检查焦点进入、层内 Tab、关闭后归还；Esc 是否可用应与提交状态一致。
- 状态不只用颜色表达；错误、加载等变化提供适当的 `role="alert"` / `role="status"`。
- 动效只服务于状态变化。侧栏当前过渡为 0.16s，组件库动效沿用主题；尊重 `prefers-reduced-motion`，不叠加整页入场动画。

### 8.3 静态对比度基线

2026-09-20 按应用 token 的 sRGB 相对亮度重新计算；下表是**纯色配对结果**，不包含透明度叠加、字体渲染、浏览器派生样式或实际屏幕测试。

| 配对                                      | 亮色    | 暗色    |
| ----------------------------------------- | ------- | ------- |
| 正文 `--fg` / `--surface`                 | 16.27:1 | 14.50:1 |
| 次要文字 `--muted` / `--surface`          | 5.76:1  | 8.15:1  |
| 占位文字 `--fg-placeholder` / `--surface` | 4.53:1  | 5.60:1  |
| 控件边界 `--border-control` / `--surface` | 3.48:1  | 3.56:1  |
| 实心主色前景 `--on-accent` / `--accent`   | 5.17:1  | 7.36:1  |
| 成功文字 / 成功标签底                     | 4.73:1  | 6.71:1  |
| 警示文字 / 警示标签底                     | 5.29:1  | 7.77:1  |
| 失败文字 / 失败标签底                     | 5.63:1  | 6.40:1  |

目标为普通文字至少 4.5:1，必要控件边界至少 3:1。静态配对通过不等于整站无障碍通过；真实页面还要检查主题切换后的主按钮、输入、下拉、抽屉、弹窗与键盘路径。

## 9. 原型的保留范围

`docs/sys-design` 保留可打开的 HTML 和配套 CSS/JS。它们拥有独立的原型 token，不参与应用打包，也不随每次产品调整同步。新的通用规范维护在本文；未来业务专属交互见 [原型交互备忘](docs/sys-design/implementation-notes.md)。

| 参考                                                                                                                  | 可以借鉴的内容                     | 当前边界                                           |
| --------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | -------------------------------------------------- |
| [P00 登录](docs/sys-design/p00-login.html)                                                                            | 表单校验、主题与会话错误           | 当前布局已采用港口配图，以应用实现为准             |
| [P04 用户管理](docs/sys-design/p04-user-management.html)                                                              | 查询、列表、编辑抽屉与影响说明     | 当前已拆成多个系统管理模块，原型中的数据范围未实现 |
| [P01 图谱](docs/sys-design/p01-graph-workbench.html)                                                                  | 搜索、筛选、画布、详情、来源与证据 | 尚未实现；不据此自动引入图谱库                     |
| [P02 AI](docs/sys-design/p02-ai-workbench.html)                                                                       | 会话、流式状态、引用与版本复核     | 尚未实现；原型不调用真实模型                       |
| [P03 任务](docs/sys-design/p03-data-tasks.html)                                                                       | 进度、结果、失败项与取消           | 尚未实现；完成进度不等于全部成功                   |
| [P05 发送方](docs/sys-design/p05-intel-collaboration.html)、[P05-B 接收方](docs/sys-design/p05b-intel-receiving.html) | 发送快照、签收、追加反馈与显式办结 | 尚未实现；实际访问需同时校验操作权限与数据范围     |

未来工作区可从已有 `--filter-w: 216px`、`--panel-w: 320px`、`--session-w: 240px` 起步，实际空间按页面核对；这些值不是当前管理抽屉宽度。图谱实体采用人员圆形、船舶圆角矩形、地点方形、事件菱形，并配颜色、汉字标和名称；来源支持与待核对关系用线型及文字区分。

不把原型的 localStorage 草稿、定时器、内存路径查询或假数据移作正式实现。也不把“设计图里有”视为新增页面、依赖或业务能力的授权。

## 10. 代码落点与待完善项

### 10.1 主题接入

当前链路为 **ThemeProvider → `data-theme` → `buildTheme()` → ConfigProvider**；自定义 CSS 直接用语义变量。

| 应用 token                             | Ant Design 对应                                                             |
| -------------------------------------- | --------------------------------------------------------------------------- |
| `--bg` / `--surface`                   | `colorBgLayout` / `colorBgContainer`、`colorBgElevated`                     |
| `--fg` / `--muted`                     | `colorText` / `colorTextSecondary`                                          |
| `--border-control` / `--border`        | `colorBorder` / `colorBorderSecondary`                                      |
| `--accent` 及 hover / active / soft    | `colorPrimary`、`colorPrimaryHover`、`colorPrimaryActive`、`colorPrimaryBg` |
| `--success` / `--warning` / `--danger` | `colorSuccess` / `colorWarning` / `colorError`                              |
| 导航、表格等专用值                     | `components.Menu`、`components.Table` 等映射                                |

Ant Design 颜色配置使用 `getComputedStyle` 读取后的**具体色值**，不能直接传 `var(--accent)` 让组件库推导色阶。字体可以引用 CSS 变量。主题切换不通过重新挂载页面完成；偏好存 `merine.theme`，没有有效偏好时按系统亮暗选择，存储失败不阻止使用。

业务组件布局放同名 CSS Module。优先使用组件公开 props 与 theme token；现有表格、表单确有局部 `.ant-*` 覆盖，新改动应限定组件根范围并核对实际 DOM，不扩成全局覆盖。表头与行高等尺寸目前也有组件内显式值，修改共享规格时要同步检查这些落点。

### 10.2 已知边界

以下是本次静态核对发现或仍需行为验收的事项，后续涉及对应链路时补齐，不代表本次已修复：

| 项目           | 当前证据与处理方向                                                                                                                                                                 |
| -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 字典标题字号   | [DictionaryListPage.module.css](apps/web/src/features/dictionaries/DictionaryListPage.module.css) 引用了未定义的 `--fs-15`；后续调整该处时选已有字号，本文不将 15px 收录为正式档位 |
| 暗色主按钮前景 | `--on-accent` 已用于自定义分页，但 `antdTheme.ts` 没有显式映射对应的实心文字色；需要在真实组件上核对派生前景，不能仅凭上面的 token 对比度表判定通过                                |
| 表单关闭保护   | 当前编辑抽屉普遍直接处理 `onClose`；重要未保存输入提醒与提交期间关闭保护尚未统一，不把设计要求写成已完成能力                                                                       |
| 尺寸与辅助技术 | 本文核对了源码与静态色值，未做本轮全站双尺寸截图、屏幕阅读器或色觉模拟验收；业务外壳仍是桌面范围                                                                                   |
| 专项工作区     | 图谱、AI、任务、情报只有原型，专属组件、数据范围和真实状态流转待实施                                                                                                               |

## 11. 开发时的最小检查

- [ ] 选用了合适的页面布局，复用了 AppShell、PageHeader 和已有相关组件，没把原型辅助控件带入正式页面。
- [ ] 使用有效的应用 token；标准组件走主题映射；亮暗主题中的正文、主按钮、边界、下拉与浮层均可读。
- [ ] 在目标页面的 1440×900、1366×768 下核对长名称、标签、操作列、滚动范围和抽屉；登录页另核对其窄屏规则。
- [ ] 加载、空数据、无匹配、失败、刷新失败和无权限有各自正确表现；没有为凑状态引入演示回退。
- [ ] 查询、分页、选中、打开/关闭表单与主题切换保持合理状态；保存等待和失败后的输入可辨认。
- [ ] 权限、字典文案、确认影响与服务端行为一致；键盘可达、焦点可见，关闭后能回到触发位置。

只检查本次影响的页面与共享能力；新增稳定视觉约定时，同步修改应用 token/组件和本文。自动测试与运行验证按 [测试规则](docs/rules/testing.md) 选取，纯文档修改只核对内容、链接、格式与差异。
